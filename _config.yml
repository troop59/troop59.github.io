# Build settings

markdown: kramdown
permalink: pretty

port: 3000

sass:
  sass_dir: assets/css
  style: :nested

gems:
 - jekyll-coffeescript

github:
  repo: girlup/girlup.github.io
  branch: master
